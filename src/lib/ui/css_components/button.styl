@require "../../style/colors.styl"

.pvc-button-primary
  height 38px
  line-height 38px

  padding 0 36px
  border-radius 19px
  background $pv_primary_color
  color #fff !important

  &:hover
    cursor pointer
    box-shadow inset 0 0 0 2px rgba(0, 0, 0, 0.1)
